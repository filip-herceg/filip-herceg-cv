// Lightweight placeholder page so the /site segment no longer drags coverage.
// Intentionally simple; future implementation can replace this.
export default function SitePlaceholder() {
	return null
}

